export interface AreaUnitData {
  id: string;
  labelTH: string;
  labelEN: string;
  ignorePrefix?: boolean;
}

export interface AreaUnit {
  id: string;
  label: string;
  ignorePrefix?: boolean;
}
